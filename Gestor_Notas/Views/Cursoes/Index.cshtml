@model IEnumerable<Gestor_Notas.Models.Curso>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Curso1)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdCarreraNavigation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdPeriodicidadNavigation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdUsuarioNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Curso1)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdCarreraNavigation.IdCarrera)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdPeriodicidadNavigation.IdPeriodicidad)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdUsuarioNavigation.IdUsuario)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdCurso">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdCurso">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdCurso">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
